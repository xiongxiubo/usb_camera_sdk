name: usb_camera_sdk
description: Reusable Flutter USB camera SDK backed by Android USB Host and libgphoto2.
publish_to: "none"
version: 0.2.0

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.flyphoto.usb_camera_sdk
        pluginClass: UsbCameraSdkPlugin
